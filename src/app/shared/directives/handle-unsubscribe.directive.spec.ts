import { HandleUnsubscribeDirective } from './handle-unsubscribe.directive';

describe('HandleUnsubscribeDirective', () => {
  it('should create an instance', () => {
    const directive = new HandleUnsubscribeDirective();
    expect(directive).toBeTruthy();
  });
});
